spring:
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate

agentic:
  visitor-hash:
    salt: test-salt
  orchestrator:
    scheduler-tick-ms: 50
    default-max-retries: 2
  agent:
    llm:
      mode: mock

logging:
  level:
    com.example.agentic: DEBUG
